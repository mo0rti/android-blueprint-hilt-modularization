<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mortitech.blueprint.auth">

    <application>
        <activity
            android:name=".presentation.activity.AuthActivity"
            android:exported="false"
            android:theme="@style/BluePrint.Theme.Base.NoActionBar">

            <nav-graph android:value="@navigation/nav_graph_auth" />

            <intent-filter>
                <action android:name="bluevelvet.blueprint.hilt.auth.open" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>
    </application>

</manifest>