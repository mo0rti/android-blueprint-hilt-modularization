<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="orangevelvet.androidblueprint.auth">

    <application>
        <activity
            android:name=".presentation.activity.AuthActivity"
            android:exported="true">

            <nav-graph android:value="@navigation/nav_graph_auth" />

            <intent-filter>
                <action android:name="orangevelvet.androidblueprint.hilt.auth.open" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>
    </application>

</manifest>