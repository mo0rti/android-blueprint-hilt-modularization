<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bluevelvet.blueprint.onboarding">

    <application>
        <activity
            android:name=".presentation.activity.OnboardingActivity"
            android:exported="false">

            <nav-graph android:value="@navigation/nav_graph_onboarding" />

            <intent-filter>
                <action android:name="bluevelvet.blueprint.hilt.onboarding.open" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>
    </application>

</manifest>
